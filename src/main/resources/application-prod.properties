# =============================================================================
# Configura??es de Produ??o (PROD)
# =============================================================================

# CORS - Frontend Vercel (nativo Quarkus + config custom)
quarkus.http.cors.origins=https://confirm-name-today.vercel.app,http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000
cors.allowed-origins=https://confirm-name-today.vercel.app,http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000

# Database - PostgreSQL (Supabase)
quarkus.datasource.db-kind=postgresql
# URL do banco de produ??o Supabase
quarkus.datasource.jdbc.url=jdbc:postgresql://db.dfgyppkvlznxrfwkqmuo.supabase.co:5432/postgres?sslmode=require
quarkus.datasource.username=postgres
quarkus.datasource.password=pgPOu0sQTcGVQ4YF

# Hibernate
quarkus.hibernate-orm.database.generation=none
quarkus.hibernate-orm.log.sql=false

# Swagger desativado em producao
quarkus.swagger-ui.always-include=true
quarkus.swagger-ui.path=/swagger
quarkus.swagger-ui.title=API Futebol

quarkus.datasource.active=false
